# 2026-09-07 交易条件审计与入场规则收紧

日期：2026-09-07
范围：信号规则审计、历史回放、实盘执行建议，以及三项入场代码修改。

本仓库只推送交易信号，不自动下单。网格、杠杆、每格利润属于交易所执行，没有写进代码。

---

## 1. 审计结论（修改前的技术条件代理回放）

对当时代码（3 根确认窗口、成交额 1.1 倍均量、`OI/MC > 90%` 即可开多）做了规则核对，并用 Binance USDT 永续 15m K 线回放 2026-05-29 至 2026-09-07（约 90 天）。本地 `data/monitor.db` 只有 2026-07-24 的 6 条快照，没有实盘成交账本，因此胜率和 ROI 来自规则回放，不是账单。

回放未加入「当时 OI/MC > 90%」和「全网价格校正 OI 增长」，因为没有历史市值序列。高 OI/MC 宇宙用的是 2026-07-24 快照里 `OI/MC > 90%` 的 12 个仍在交易的名字。

因此，下列胜率和 ROI 只描述价格、EMA、成交额和 RSI 条件的代理表现，不是当前完整开多规则的策略回测。币种池来自回放区间中段的单次快照，并要求审计时仍在交易，存在时点偏差和幸存者偏差；结果不能用于证明 `200%`、当根突破或 `2×` 成交额是最优参数。

### 1.1 胜率与期望

| 样本 | 笔数 | 胜率 | 均收益 | 中位数 | 说明 |
|---|---|---|---|---|---|
| 高 OI/MC 宇宙 | 535 | 32.0% | +0.95% | **-0.80%** | 典型单笔是亏的 |
| 流动性更好的币（BTC/ETH/SOL 等） | 724 | 28.0% | -0.10% | — | 盈亏比 0.85，技术层为负 |

高 OI/MC 宇宙前 5 笔贡献了约 72% 的毛利润；AKE 两笔约 +107%、+97%。去掉前 5 笔后均收益只剩 +0.27%。OI/MC 最高的 ESPORTS（825%）、EVAA（403%）胜率约 21%，合计约 -78%。

这套规则不是高胜率策略，而是少数大赚、多数小亏。中位数为负，均值被极值拉正。

### 1.2 组合 ROI（扣 0.25% 成本后）

对上述 12 个高 OI 名字、每个币最多一笔：

| 算法 | 90 天 ROI | 最大回撤 |
|---|---|---|
| 12 等权、固定仓位 | +31.4% | -5.4% |
| 12 等权、各自复利 | +71.7% | -8.0% |
| 每笔按 2×ATR 冒 1% 本金 | +22.0% | -35.4% |
| 每笔冒 2% 本金 | +20.4% | -60.0% |
| 去掉 AKE | +7.2% | — |
| 再去掉 LAB | **-3.8%** | — |

前 5 笔约占净利 97%。流动性更好的币 90 天约 -18%。等权回撤看起来小，是因为资金摊在 12 个名字上；按信号逐笔下单时，路径更接近 1%～2% 风险那两行。

三项新规则尚未做包含真实历史 OI/MC、价格校正聚合 OI 和动态币种池的组合回放。上线只能视为风险政策收紧；在增加仓位前，至少需要保存实际信号账本，并用时间外样本比较原规则、三项单独修改和三项组合后的信号数、胜率、收益分布及最大回撤。

### 1.3 规则层面的主要问题

1. 页面把 `OI/MC > 200%` 标成「埋伏」，开多条件却仍是 `> 90%`，埋伏区照样发【做多】。
2. 允许最近 3 根内确认突破，约 59% 的入场是追价，均收益从约 1.8% 掉到 0.4%。
3. 成交额只需 1.1 倍 20 周期均量，假突破容易过。
4. RSI 只要求回升、没有上限；止损/止盈看收盘价不看影线。这两点这次没有改代码。

---

## 2. 实盘执行建议（未改代码）

当时的执行方式：200U、10x、等差网格、每格利润 0.3%～0.5%。

不建议把 10% 止盈 / 30% 止损，或 10x 密网写进本仓库。原因：

- 10% 止盈 / 30% 止损打平需要约 75% 胜率，回放胜率约 32%，期望为负。
- 10% 固定止盈会切掉 AKE 这类右尾，而右尾几乎是全部纸面 ROI。
- 10x 约在价格逆向 8%～10% 强平；这批币 15m 上出现过单根 -15%、四根 -23%。
- 0.3%～0.5% 格距小于小币点差、滑点和资金费量级，单边行情会把格子堆满。

建议（仍由交易员在交易所执行，本仓库不下单）：

- 账户实际杠杆约 2x，名义仓大约 400U；200U 不要打满成 2000U。
- 逐仓、同一时间只做一个币。
- 只在「可以做多」时开**多向**网格；格距 ≥ 1%，约 6～8 格。
- 「必须退出」或跌破 EMA200 时整网市价撤掉，不要等 30% 或强平。
- `OI/MC > 200%` 不要再开多向网格（本次代码已禁止该区信号）。

---

## 3. 本次代码修改

目标：让信号少发、少追价、避开最拥挤的埋伏区。系统仍然只发信号，不执行网格。

### 3.1 埋伏区禁止开多

`OI / 市值 > 200%`（与页面埋伏阈值 `AMBUSH_OI_TO_MARKET_CAP_RATIO = 2` 一致，严格大于）时：

- 不再发送【做多】或【恢复做多】。
- 无活动交易状态时，不依赖 Binance K 线，扫描直接归入「停止做多」，原因 `oi_to_market_cap_in_ambush_zone`。
- 已有 `long` / `no_add` 仓位时传入完整持久化状态并加载 K 线，回放 ATR 止损和移动保护；即使快照不完整、未映射、跌出当前比较范围或当前比较项暂缺 Binance 合约也继续执行，满足退出条件时「必须退出」优先。当前快照识别到的 Binance 合约会在 K 线加载前标准化；即使本轮 K 线请求失败或历史回放因时间缺口失败，也只保存该安全映射，不推进保护价或 K 线检查点，下一轮仍能使用该合约继续风控。无企业微信时也会保存安全的移动保护和 K 线检查点；尚未派发的历史退出不会越过触发 K 线推进检查点，避免下一轮价格恢复后吞掉退出信号。
- 已有 `long` 仓位改为 `no_add`（禁止补仓），不因此强制「必须退出」。
- 完整快照已能独立确认 `OI/MC` 越界或活动币比较数据缺失时，即使 Binance 合约缺失或 K 线请求失败，也会保留错误记录并把 `long` 降为 `no_add`；K 线不可用只会阻止退出检查，不会阻止保守地停止加仓。正常派发、无企业微信扫描和派发异常回退采用相同规则。
- 活动币同时缺少当前比较项和持久化 Binance 交易对时，仍会保留合约错误并执行 `long → no_add`。K 线已经加载但历史回放出现时间缺口时，也会先应用可独立确定的 OI/数据停止原因，同时保持保护价、最高价和 K 线检查点不变，等待连续历史恢复后重新回放退出风控。
- 当前快照刚补齐 Binance 交易对且历史回放命中必须退出时，即使退出通知失败，也会先保存回放前的安全交易对映射；不会保存回放产生的保护价、最高价或 K 线检查点，下一轮比较项缺失时仍可加载 K 线并重放退出。
- `reentry_cooldown` 在无企业微信、派发异常回退扫描和不完整快照中仍生效；当前可识别的 Binance 合约会被回填，冷却结束后继续要求新的当根突破和全部入场条件。
- 没有活动交易状态的普通候选只会归入「可以做多」或「停止做多」，不会仅因 EMA 退出条件产生虚假的「必须退出」。
- 必须退出仍只由 ATR 止损、移动止盈、EMA 缓冲、连续两根跌破 EMA 触发。
- 刚好等于 200% 仍允许开多。

实现：`trade_dispatch.py` 新增 `_oi_ratio_trade_reasons()`，同时覆盖 `≤ 90%` 和 `> 200%`。

### 3.2 只认当根 EMA200 突破

| 项 | 修改前 | 修改后 |
|---|---|---|
| `EMA_BREAKOUT_LOOKBACK` | 3 | **1** |
| 含义 | 最近 3 根已收盘 15m 内任意一次上穿即可 | 必须是**当前这根**收盘由 ≤ EMA200 上穿到 > EMA200 |

`entry_reasons` 同时拒绝 `breakout_ago >= 1`，避免手工构造的过期突破被当成有效。

### 3.3 成交额门槛提到 2 倍

| 项 | 修改前 | 修改后 |
|---|---|---|
| `VOLUME_BREAKOUT_MULTIPLIER` | 1.1 | **2.0** |
| 含义 | 当前 15m USDT 成交额 > 前 20 根均量的 1.1 倍 | 严格高于前 20 根均量的 **2 倍** |

仍不要求高于上一根成交额。等于 2 倍不算突破。

### 3.4 未改的部分

- 仍只做多，不做空。
- RSI 仍只要求回升，无上限、无下限。
- 初始止损仍为入场价 − 2×ATR；移动止盈仍为 +3 ATR 激活。
- 止损/止盈仍看已收盘价，不看影线。
- 杠杆参考文案仍为 2～3 倍，没有写成 10x。
- 没有自动下单、没有成交盈亏账本。

---

## 4. 修改后的开多条件

须**同时**满足：

1. 快照完整。
2. `90% < OI / 市值 ≤ 200%`。
3. 价格校正后的全网聚合 OI 较约 15 分钟前增加。
4. **当根**已收盘 15m 上穿 EMA200，且收盘仍在 EMA200 上方。
5. 当前 EMA200 高于 5 根前的 EMA200。
6. 当前 15m USDT 成交额 > 前 20 根均量的 2 倍。
7. RSI(14) 严格高于上一根。
8. 不在必须退出后的冷却期内；冷却结束后的突破必须发生在当根，且不早于冷却结束。

停止开多：`OI/MC ≤ 90%`，或 `OI/MC > 200%`，或收盘不高于 EMA200。

必须退出：触及活动保护价，或收盘低于 `EMA200 − 0.5×ATR`，或连续两根收盘低于各自 EMA200。

---

## 5. 改动文件

| 文件 | 内容 |
|---|---|
| `src/crypto_oi_monitor/trading.py` | `EMA_BREAKOUT_LOOKBACK = 1`，`VOLUME_BREAKOUT_MULTIPLIER = 2.0`，入场拒绝过期突破 |
| `src/crypto_oi_monitor/trade_dispatch.py` | `_oi_ratio_trade_reasons`；埋伏区无仓位时跳过 K 线，活动仓位以完整状态回放退出风控；不完整、缺失比较项或当前比较项缺少 Binance 合约时仍用持久化交易对扫描/派发；单币合约缺失不会中断整批扫描；扫描安全推进移动保护、`long → no_add` 和冷却状态；扫描与派发均在 K 线加载前标准化旧 Binance 交易对，K 线请求或历史回放失败也保留映射；不完整快照保留冷却状态；未派发退出保持可重放 |
| `app.py` | 无企业微信时传入 `long` / `no_add` / `reentry_cooldown` 状态并逐币保存扫描产生的安全状态更新，单币写入失败不会跳过后续币；推送异常后重新读取最新状态再回退扫描，二次读取失败时使用刷新开始时的状态 |
| `src/crypto_oi_monitor/storage.py` | 所有 SQLite 事务使用保证关闭连接的上下文，避免 Windows 文件句柄残留 |
| `src/crypto_oi_monitor/notifier.py` | 企业微信条件说明与中文原因 |
| `frontend/src/App.jsx` | 扫描说明、卡片文案、原因映射；未加载的 K 线指标显示为“—” |
| `README.md` | 开多 / 停止 / 恢复 / 冷却 / 扫描说明 |
| `tests/test_trading.py` | 当根突破、2 倍成交额边界 |
| `tests/test_trade_dispatch.py` | 埋伏区不开多、等于 200% 仍可开、活动仓位 ATR/移动保护退出优先、不完整且缺失比较项仍扫描、持久化 Binance 交易对回退、K 线请求、历史回放和退出通知失败后的跨刷新映射保留、不完整快照冷却状态、K 线或交易对不可用及历史回放缺口时仍按 OI 越界或比较数据缺失执行 `long → no_add`、单币缺失隔离、未派发退出重放、冷却、无仓位不退出；有效信号 K 线成交额改为 2 倍以上 |
| `tests/test_app.py` | 无企业微信和异常回退时传入完整交易状态并保存安全风控更新；无企业微信时单币状态写入失败不影响后续币；推送异常回退重新读取最新状态，二次读取失败时使用初始状态 |
| `tests/test_storage.py` | SQLite 成功提交和异常回滚后均显式关闭连接 |
| `tests/test_notifier.py` | 文案断言 |
| `frontend/src/sources.test.js` | 原因文案 |
| `frontend/src/trade-condition-panel.test.jsx` | 面板说明与空 K 线指标渲染 |
| `frontend/dist/*` | 本机已重新 `npm run build`；该目录被 Git 忽略，不会随提交部署 |

新增原因码：`oi_to_market_cap_in_ambush_zone` → 「OI / 市值已高于 200%，禁止开多」。

---

## 6. 验证

```powershell
cd D:\code-web3\06-trading-research\CryptoOIMonitor
$env:PYTHONPATH = 'src'
python -m unittest tests.test_trading tests.test_trade_dispatch tests.test_notifier
python -m compileall -q app.py src\crypto_oi_monitor

cd frontend
npm test
npm run build
```

交易 / 调度 / 通知相关测试 110 个通过；存储测试 20 个通过；后端全量 236 个通过；前端 Vitest 16 个通过；生产构建成功。

SQLite 连接已改为事务结束后显式关闭，Windows 临时数据库清理不再依赖垃圾回收。

---

## 7. 上线

修改的是运行中的信号逻辑和前端文案。若通过 Git 更新目标机器，`frontend/dist` 不在版本控制中，必须先在目标机器执行：

```bash
cd frontend
npm ci
npm run build
cd ..
bash scripts/stop.sh
bash scripts/start.sh
```

同一台机器若已完成前端构建，也仍需重启 Python 服务，后端信号规则才会生效。

预期：可以做多的币会变少；埋伏区只会停开、不会新开；通过 1.1～2 倍均量或在突破后两根才确认的信号会被过滤。不保证胜率或 ROI 提高，只是把信号从「密网容易被扫」往「更少、更晚、避开最拥挤」收了一档。
